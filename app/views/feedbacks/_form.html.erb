<div class="container-fluid">
  <div id="content">
    <div class="jumbotron">
      <%= form_for([@user, @group, @feedback]) do |f| %>
          <% if @feedback.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(@feedback.errors.count, "error") %> prohibited this feedback from being saved:</h2>

                <ul>
                  <% @feedback.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
          <% end %>
          <div class="row">

          <div class="col-xs-2" style="width: 100%">
            <div class="form-group">
              <label for=:feedbacktext style="font-size: 3rem">Kommentar:</label><br/>
              <%= f.text_area :feedbacktext , class:"form-control", rows:20, style:"font-size: 2.2rem; resize:none"%>
            </div>
          </div>
          </div>


          <div class="actions">
            <%= link_to 'Zurück', frontend_path, class:"btn btn-lg btn-info",style:"font-size: 1.4em;margin-right:20px"%>

            <%= f.submit "Kommentar abschicken",  class: "btn btn-lg btn-info", style:"font-size: 1.4em", data: { confirm: 'Kommentar speichern?' }%>
          </div>
          <div>
          </div>
      <% end %>
    </div>

  </div>
</div>