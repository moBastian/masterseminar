var user = "<%= @user.id%>";
var group = "<%= @group.id%>";
var buildhref = "#tabs a[href='/users/" + user + "/groups/" + group + "/students']"
$('#students').html("<%= escape_javascript(render 'index')%>");
$(buildhref).tab('show');




